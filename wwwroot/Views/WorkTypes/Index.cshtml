@model IEnumerable<CommunityBackend.Models.WorkType>

@{
    Layout = "~/Views/Shared/_layoutForFrame.cshtml";
    ViewBag.Title = "手艺类别管理";
}

<h2>手艺类别</h2>

<p>
    @Html.ActionLink("创建", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("编辑", "Edit", new { id = item.WorkTypeID }) |
            @Html.ActionLink("查看", "Details", new { id = item.WorkTypeID }) |
            @Html.ActionLink("删除", "Delete", new { id = item.WorkTypeID })
        </td>
    </tr>
}

</table>
